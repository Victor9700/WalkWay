services:
  - type: web
    name: walkway
    env: php
    repo: https://github.com/Victor9700/WalkWay
    buildCommand: "composer install"
    startCommand: "php artisan serve --host 0.0.0.0 --port 10000"
    plan: free
    autoDeploy: true
